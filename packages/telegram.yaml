# Telegram
telegram_bot:
  - platform: broadcast
    api_key: !secret telegram_api_key
    allowed_chat_ids:
      - !secret telegram_chat_id

notify:
  - name: telegram_alex
    platform: telegram
    api_key: !secret telegram_api_key
    chat_id: !secret telegram_chat_id
